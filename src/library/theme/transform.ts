import { BeautifyCase } from '.';

export const transforms: Record<BeautifyCase, string> = {
  uppercase: 'uppercase',
  lowercase: 'lowercase',
  capitalize: 'capitalize',
  'normal-case': 'none',
};
