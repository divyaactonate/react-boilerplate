import cn from 'clsx';


interface {{properCase name}}StylesProps {

}

export const fetchStyles = (props: {{properCase name}}StylesProps) => {
  const {  } = props;
  const common = ``;

  const {{lowerCase name}} = cn(
    common,
    
  );

  const classes = {
    {{lowerCase name}},
  };
  return classes;
};
