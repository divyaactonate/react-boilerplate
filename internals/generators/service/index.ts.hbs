import { ApiCaller } from '../apiCaller';
import { Logger } from '@utils/logger';

export class {{pascalCase name}}Service {
  private static API_URL = ``;
  static getAll = async () => {
     try {
      const res = await ApiCaller.httpGet(`${({{pascalCase name}}Service).API_URL}/`);
      Logger.debug('{{pascalCase name}}Service.getAll() ', res);
      return res;
    } catch (err) {
      Logger.error('{{pascalCase name}}Service.getAll() ', err);
      throw err;
    }
  };
  static add = async (data: any) => {
     try {
      const res = await ApiCaller.httpPost(`${({{pascalCase name}}Service).API_URL}/`,data);
      Logger.debug('{{pascalCase name}}Service.add() ', res);
      return res;
    } catch (err) {
      Logger.error('{{pascalCase name}}Service.add() ', err);
      throw err;
    }
  };
  static delete = async (id: number) => {
     try {
      const res = await ApiCaller.httpDelete(`${({{pascalCase name}}Service).API_URL}/${id}`);
      Logger.debug('{{pascalCase name}}Service.delete() ', res);
      return res;
    } catch (err) {
      Logger.error('{{pascalCase name}}Service.delete() ', err);
      throw err;
    }
  };
}
