import cn from 'clsx';
import { useMemo } from 'react';


interface {{properCase name}}StylesProps {

}

export const getStyles = (props: {{properCase name}}StylesProps) => {
  const {  } = props;
  const common = ``;

  const {{lowerCase name}} = cn(
    common,
    
  );

  const classes = {
    {{lowerCase name}},
  };
  return classes;
};

export const useStyles = (props: {{properCase name}}StylesProps) => {
  const {  } =props;
  return useMemo(() =>getStyles({}),[]);
};